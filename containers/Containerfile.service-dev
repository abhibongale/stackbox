FROM docker.io/library/ubuntu:24.04
ARG SERVICE_NAME
ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 python3-pip python3-venv python3-dev \
    gcc libffi-dev libssl-dev libpq-dev && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

RUN python3 -m venv /opt/stackbox/venv
ENV PATH="/opt/stackbox/venv/bin:$PATH"

COPY ./source /opt/stackbox/source
RUN pip install --no-cache-dir -e /opt/stackbox/source
RUN pip install --no-cache-dir pymysql python-memcached uwsgi
